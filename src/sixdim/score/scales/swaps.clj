(ns sixdim.score.scales.swaps
  (:use overtone.core)
  (:require 
    ; [sixdim.state_defs :as state_defs]
    [sixdim.atoms :as atoms] 
    [sixdim.common_fns :as common_fns]
    ; [sixdim.score.score :as score]
    ; [sixdim.score.melody :as melody]
    ; [sixdim.score.undo :as undo]
    )
  (:gen-class))

; (scale_swaps/selection_to_scale "GDi") ;G dom in Cmaj scale
(defn selection_to_scale [scale_id]
  scale_id)

